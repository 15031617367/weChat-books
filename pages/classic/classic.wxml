<!--pages/classic/classic.wxml-->
<B-like 
    bind:like="onLike"
    likeBol="{{classic.like_status}}" 
    follow="{{classic.fav_nums}}"
/>
<B-movie 
    img="{{classic.image}}" 
    content="{{classic.content}}"
/>

